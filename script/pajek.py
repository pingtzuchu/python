f = open(test.txt )
i=1
vertices={}
edges=[]
tempEdge=[]
arcs{}
for line in f.readlines:
      names=line.split
      for j in range(1):
            if names[j] in vertices:
                  pass
            else:
                  vertices.append(names[0]:str(i))
                  i += 1
      if len (name[2]) > 0:
            edges.append([vertices[name[0]], vertices[name[1]], name[2]])
      else:
            edges.append(([vertices[name[0]], vertices[name[1]])
       
